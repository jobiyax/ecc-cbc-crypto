[project]
name = "ecc-cbc-crypto"
version = "0.1.0"
description = "Crypto ECC + ECDH + CBC en Python pur"
readme = "README.md"
authors = [
    { name = "jobiyax", email = "contact@jobiyax.blog" }
]
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.29,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.2",
]
